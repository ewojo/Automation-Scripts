#!/bin/bash

#This is a bash script that will output the string "hello" when run. The first line, "#!/bin/bash", is called the shebang and is used to specify the interpreter for the script, in this case /bin/bash. The second line, "echo", is a command that will print the following string to the terminal.

echo "hello"
